"""CLI entry point for ReOS."""
import sys


def main() -> int:
    """ReOS command-line interface."""
    print("ReOS v0.1.0 — Natural language Linux system control")
    print("TUI coming in Phase 3. Use --help for options.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
